import { FilterBySearchTermPipe } from './filter-by-search-term.pipe';

describe('FilterBySearchTermPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterBySearchTermPipe();
    expect(pipe).toBeTruthy();
  });
});
